<%= form_for @user do |f| %>

  <%= f.label :first_name %> <%= @user.first_name %>

  <br>

  <%= f.label :last_name %> <%= @user.last_name %>

  <br>

  <%= f.label :email %> <%= @user.email %>

<% end %>


<%= link_to "Back", users_path() %>
